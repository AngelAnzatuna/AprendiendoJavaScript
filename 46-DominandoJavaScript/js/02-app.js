obtenerCliente('Angel');

function obtenerCliente(nombre) {
    console.log(`El nombre del cliente en es ${nombre}`);
}

obtenerCliente2('Alexis');
const obtenerCliente2 = function(nombre) {
    console.log(`El nombre del cliente en es ${nombre}`);
}
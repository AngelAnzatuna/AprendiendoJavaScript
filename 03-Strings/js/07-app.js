const producto = 'Monitor 20 Pulgadas ';
console.log(producto.split(' ')); 

/* toUpperCase() : para transformar el texto en mayúsculas */
console.log(producto.toUpperCase());
/* toLowerCase() . transforma el texto en minúsculas */
console.log(producto.toLowerCase());

/* toString() : cambia un tipo numerico o bolean a string */
const precio = 200;
console.log(precio);
console.log(precio.toString());


// Inicializar una variable con un valor
var producto = 'Monitor de 24 pulgadas';
console.log(producto);

// Las variables se puede reasignar
producto = 'Monitor de 19 pulgadas';
console.log(producto);

// JavaScript es un lenguaje de tipo dinamico no es especifica el tipo de dato
producto = 20;
console.log(producto);

// Se puede inicializar sin valor y asignarlo después
var disponible;
disponible = true;

disponible = false;

/* Inicializar multiple variables : como se ve en el ejemplo
se debe colocar la coma despues de poner el valor a la variable
y poner el nombre de la nueva variable, el nuúmero de variables
que deseemos*/
var categoria = 'Computadoras',
    marca = 'Marca Famosa',
    calificación = 5;

/* Reglas
* No se puede inicializar con números
var 99dias; */

// Si tienes 2 nombres la 2da palabra iniciar con mayuscula
var nombreProducto;